require 'spec_helper'

describe Participant do

  it "should create new users from params"
  it "should return an array of ids for the users it creates"
  it "should not create users who already exist"

  it "should invite users who are new to a project"
  it "should not invite users who were already part of a project"
  it "should not invite cunconfirmed users"

end