<div class="well">
  <h3><%= @ticket.title %></h3>
  <h4><%= @ticket.parent %></h4>
  <div>
    <%= @ticket.body %>
  </div>

</div>

<%= render :partial => 'version', :collection => @ticket.versions %>
<%= render :partial => 'version', :object => @ticket %>

<%= render :partial => 'form', :locals => {:comment => true} %>

