<div class="well">
  <h3><%= @ticket.title %></h3>
  <h4><%= @ticket.ticketable %></h4>
  <div>
    <%= @ticket.body %>
  </div>

</div>

<%= render :partial => '/comments/comment', :collection => @ticket.comments %>

<%= render :partial => '/comments/new', :locals => {:comment => @ticket.comments.build()} %>

