<div class="nested-fields">
  <%= f.input :party, collection: Integration::PARTIES, include_blank: false %>
  <%= f.input :key if f.object.persisted? %>
  <%= f.input :enabled, as: :radio_buttons, collection: {enabled: true, disabled: false} %>
  <p>For new API keys, they key will be generated once we save the record</p>
  <%= link_to_remove_association raw("&times;"), f %>
</div>
