<ul class="nav nav-pills nav-stacked">
  <% if action_name != 'index' %>
    <li>
      <%= link_to 'back to project', project_path(@sprint.project) %>
    </li>
  <% end %>
  <% if action_name == 'show' %>
    <li>
      <%= link_to 'edit this sprint', edit_project_sprint_path(@sprint.project,@sprint) %>
    </li>
    <li>
      <%= link_to 'remove this sprint', project_sprint_path(@sprint.project,@sprint), :method => :delete, :data => {:confirm => "Are you sure you want to delete this sprint?"} %>
    </li>
    <li>
      <%= link_to 'add a new ticket', new_ticket_path(:project_id => @project, :sprint_id => @sprint) %>
    </li>
  <% end %>
  <% if action_name == 'index' %>
    <li>
      <%= link_to 'add a new sprint', new_project_sprint_path(@sprint.project) %>
    </li>
  <% end %>
</ul>

