<ul class="nav nav-pills nav-stacked">
  <% if action_name != 'index' %>
    <li>
      <%= link_to 'back to project', project_path(@feature.project) %>
    </li>
  <% end %>
  <% if action_name == 'show' %>
    <li>
      <%= link_to 'edit this feature', edit_project_feature_path(@feature.project,@feature) %>
    </li>
    <li>
      <%= link_to 'remove this feature', project_feature_path(@feature.project,@feature), :method => :delete, :confirm => "Are you sure you want to delete this feature?" %>
    </li>
    <li>
      <%= link_to 'add a new ticket', new_ticket_path(:feature_id => @feature) %>
    </li>
  <% end %>
  <% if action_name == 'index' %>
    <li>
      <%= link_to 'add a new feature', new_project_feature_path(@feature.project) %>
    </li>
  <% end %>
</ul>

