<ul class="nav nav-stacked">
    <%= action_back_to(@project) %>
    <%= action_edit(@feature) %>
    <%= action_remove(@feature) %>
    <%= action_add(@project.features.new()) %>
    <%= action_add(@project.tickets.new()) %>
</ul>
